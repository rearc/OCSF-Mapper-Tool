streamlit>=1.30
streamlit-ace>=0.1.1
databricks-sdk>=0.30
# ocsf_mapper is now a source package in this repo (see src/ocsf_mapper/).
# Installed editable so app.py can `import ocsf_mapper` directly.
-e .
